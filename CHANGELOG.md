# Changelog

All notable changes to this project are documented here.
Format: one entry per merged PR, newest first.

---

## [Unreleased]

### Added
- INF-1: CMake project skeleton — SDL2 and SDL2_mixer via FetchContent (static,
  WAV-only mixer); five library targets (`lib_game`, `lib_platform`, `lib_rendering`,
  `lib_input`, `lib_audio`); `asteroids` executable (exits immediately); `tests/`
  subdirectory scaffold; `.gitignore`
